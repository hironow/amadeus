# OS
.DS_Store
# build artifacts
/dist/
# test
coverage.out
# amadeus runtime state — ignore contents individually
# git-tracked: insights/, archive/, config.yaml (per S0030)
.gate/.run/
.gate/events/
.gate/inbox/
.gate/outbox/
.gate/skills/
.gate/.otel.env
.gate/.gitignore
# environment-specific local files (must not contain full paths)
*.local.*
# claude local settings
.claude/*.local.*
# git worktrees
.worktrees/
# plan drafts (ephemeral)
docs/plans/
# golang
.gocache/
# firebase local debug log
firebase-debug.log
